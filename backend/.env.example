# Environment Variables for Password Reset Email Functionality

# Frontend URL for reset links
FRONTEND_URL=http://localhost:3000

# Email Configuration (SMTP)
# For Gmail, you'll need to use an App Password instead of your regular password
# Enable 2FA first, then generate an App Password: https://support.google.com/accounts/answer/185833

# SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# From Email Address
FROM_EMAIL=noreply@colorra.com

# Alternative email services:
# 
# For SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
#
# For Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=your-mailgun-smtp-username
# SMTP_PASS=your-mailgun-smtp-password
#
# For development/testing:
# You can use services like Mailtrap.io or MailHog for testing
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your-mailtrap-username
# SMTP_PASS=your-mailtrap-password

# Instructions:
# 1. Copy this file to .env in your backend directory
# 2. Replace the placeholder values with your actual email configuration
# 3. Make sure .env is in your .gitignore file (it should already be there)
# 4. Restart your backend server after updating environment variables
